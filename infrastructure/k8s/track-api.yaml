apiVersion: apps/v1
kind: Deployment
metadata:
  name: track-api
  namespace: sentinel-sda
spec:
  replicas: 1
  selector:
    matchLabels:
      app: track-api
  template:
    metadata:
      labels:
        app: track-api
    spec:
      containers:
        - name: track-api
          image: sentinel-sda-track-api:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: track-api
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: sentinel-jwt
                  key: JWT_SECRET
            - name: JWT_ISSUER
              valueFrom:
                secretKeyRef:
                  name: sentinel-jwt
                  key: JWT_ISSUER
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: track-api
  namespace: sentinel-sda
spec:
  selector:
    app: track-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
